import React from 'react';
import { render, screen } from '@testing-library/react';
import Home from '../pages/Home/Home';

test('renders learn react link', () => {
  // render(<Home />);
  // const displayTitle = screen.getByRole
});
